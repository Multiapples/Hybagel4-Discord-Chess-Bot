# Hybagel4-Discord-Chess-Bot
A discord bot for playing chess.

## Note for if you are trying to download and run for yourself:
1. Go into src/ and install discord.js with npm.
2. Create a file called token.json in src/ and paste the following in it (replace your-token-here with your bot token): { "token": "your-token-here" }